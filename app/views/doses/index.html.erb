<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <h1>Doses list</h1>
      <ul>
        <% @doses.each do |dose| %>
        <li><%= link_to dose.name, dose_path(dose) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
